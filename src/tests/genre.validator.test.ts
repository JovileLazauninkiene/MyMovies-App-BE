// import validation from '../validators/genre.validator';

// test('genres is numeric array', () => {
//   expect(validation()).toBe('array');
// });

// test('genres is a number', () => {
//   expect(validation()).toMatch(/[0-9]+/);
// });

// test('genres is a integer more than 0', () => {
//   expect(validation()).toBeGreaterThan(0);
// });
